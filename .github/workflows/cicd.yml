name: CICD pipline

on: 
  workflow_dispatch:

jobs:

  code:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        run: echo "code clone from github"

  build:
    needs: [code]
    runs-on: ubuntu-latest
    steps:
      - name: Build docker container
        run: echo "Docker container build"
        
  test:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - name: Test the code
        run: echo "Testing the container"
        
  deploy:
    needs: [test,build]
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the app
        run: echo " docker composed deploy the app"
